
///////////////////////////////////////////////////////
////////////// INTEGRATION TEST HEADER ////////////////
/////////// this must run before any tests ////////////
///////////////////////////////////////////////////////
process.env.TKD_ENV = 'testing';
process.env.TKD_LOGLEVEL = 'debug';

app = require('../../app.js');
expect = require('expect.js');
request = require('supertest');

before(function(done) {		
	// Wait for app to be ready			
	if(app.get('state') !== "ready") {	
		app.once('ready', done);
	} else {									
		done();										
	}
});
///////////////////////////////////////////////////////
///////////////////// END HEADER //////////////////////
///////////////////////////////////////////////////////


